# LESSONS.md — UniProof

> **MANDATORY READ RULE:** This is the first manual project file every agent must read at the start of every session, before code inspection, planning, or edits.
>
> Record only reusable mistakes, explicit user corrections, recurring environment pitfalls, root causes, and durable preventive actions. Keep project status and temporary plans out of this file.

## Entry Format

| Date | Context | What happened | Root cause | Durable corrective action |
| --- | --- | --- | --- | --- |

## Log

| Date | Context | What happened | Root cause | Durable corrective action |
| --- | --- | --- | --- | --- |
| 2026-08-16 | Repository initialization | Project governance was created with `LESSONS.md` as the mandatory first manual session read. | Cross-agent work needs a durable way to avoid repeating known mistakes. | Keep this read rule in root `AGENTS.md`, and append only lessons that are likely to matter again. |
| 2026-08-16 | Windows workspace creation | A nested PowerShell wrapper mangled a variable and a second command used an unsupported `New-Item -LiteralPath` form in the connected shell. | Command quoting and shell capability were assumed instead of using the simplest native path operation. | On this host, prefer explicit `cmd.exe` directory creation for new paths with spaces, or verify PowerShell syntax before relying on nested quoting. |
| 2026-08-16 | Next.js metadata icon | Writing `app/icon.svg` through the generic file writer produced bytes that Next.js rejected as invalid UTF-8, causing the dev route to return HTTP 500 until the asset was rewritten explicitly as UTF-8 bytes. | The SVG path was treated differently from ordinary text and its encoding was not verified after creation. | For Next.js SVG metadata assets on this host, write with an explicitly UTF-8-safe method and immediately verify a live route or production build before continuing. |
| 2026-08-16 | IPv6 SSRF review | A hand-maintained IPv6 special-use blacklist missed newly assigned/reserved ranges and encoded `2001:10::/28` incorrectly. | The policy treated an evolving registry as a static blacklist and lacked a fail-closed allocation boundary. | For outbound IPv6 safety, verify current IANA allocations during security reviews, prefer a conservative fail-closed public allocation gate plus explicit special-use exclusions, and keep regression cases for newly observed registry changes. |
| 2026-08-16 | Outbound validation error sanitization | URL validation failures removed credentials but still echoed path/query/fragment data, allowing secret-bearing URL material to appear in error payloads. | Error-safe URL handling sanitized only userinfo and treated the rest of a parseable URL as harmless metadata. | For externally supplied URLs, failure/error representations must minimize to origin-level HTTP(S) metadata and strip path, query, fragment, and opaque-scheme payloads; preserve the full canonical URL only on validated success/internal retrieval paths. |
| 2026-08-16 | Phase 2A contract re-review | Research contracts that extended permissive Phase 1 Zod schemas silently inherited unbounded fields and unknown-key stripping, allowing malformed model/provider payloads through stricter downstream trust boundaries. | Reusing a base schema does not automatically preserve the validation guarantees required by a narrower trust boundary. | At model/provider and evidence boundaries, explicitly override inherited fields with local bounds/transforms, make boundary objects strict, and regression-test unknown keys, whitespace-only strings, oversized values, canonical identities, and cross-record aggregates. |
| 2026-08-16 | Phase 2D review-agent routing | GLM review requests repeatedly returned HTTP 429, and retrying failed child deployments created noisy duplicate attempts instead of useful review evidence. | Provider/model capacity failure was treated as recoverable delegation work rather than as a terminal failure for that provider/model in this task. | Treat a child 429 as terminal for that provider/model, stop retrying failed children, use a GPT variant only after explicit authorization, cap delegation to one bounded reviewer per required review, and interrupt each child immediately after completion or error. |
| 2026-08-16 | Phase 2D Unicode-bound review | Generated segment/candidate IDs and returned model provenance were bounded with Unicode code-point counting while the live Zod contracts bound JavaScript UTF-16 string length, so astral characters could pass the generator/provider check and then fail the trusted schema. | Different layers used different definitions of string length. | For generated identifiers and trusted metadata, enforce the same length metric as the receiving contract and add astral-Unicode regressions at the boundary rather than assuming code-point-safe slicing also satisfies schema length. |
| 2026-08-16 | Phase 2D bounded provider transport | Awaiting `ReadableStream.cancel()` on rejected/oversize provider bodies could let an untrusted stream keep a bounded failure pending indefinitely, and a single undifferentiated `budget` result could incorrectly make provider-local exhaustion terminal for the whole fallback chain. | Cleanup promises and budget scope were treated as part of the primary control path instead of bounded auxiliary state. | Make failure-path stream cancellation best-effort/non-blocking, keep provider-local and total budget scope distinct internally, and regression-test that provider-local exhaustion fails over while total exhaustion stops dispatch. |
| 2026-08-17 | Final-review subagent liveness | A reviewer was closed and fallback was triggered after a bounded parent-side wait elapsed even though the child had returned neither a result nor an error. | Lack of completion within one polling/wait window was incorrectly inferred to mean timeout/failure instead of checking whether the child was still actively thinking. | A wait window expiring with no result/error is not failure. Inspect child status; if it is running, keep it alive and continue checking. For the user's reviewer policy, the timeout/rate-limit fallback trigger means an explicit HTTP 429/API rate-limit response, not elapsed wall-clock time. Only explicit terminal child errors/failures may trigger the permitted model fallback. |
| 2026-08-17 | Phase 2E temporal/provenance review | Future effective dates were treated as current, opaque year strings were ordered from their first four digits, and candidates with broken source/document references could collapse into evidence `unknown`. | The evidence gate inferred temporal/provenance facts from partial representations instead of requiring a canonical comparison or complete application-owned provenance. | Order only canonical recognized periods, distinguish future/unknown from outdated, and make broken candidate provenance operationally incomplete. Evidence `unknown` is valid only after the required operational work completed successfully. |
| 2026-08-17 | Phase 2E authority/independence review | Anecdotal/ranking material and differently labeled university pages could manufacture corroboration, while authoritative primary support could be downgraded to `corroborated` because the gate checked source count before authority. | Source-type eligibility, owner identity, and evidence-state precedence were evaluated independently instead of as one deterministic policy. | Exclude non-reliable source types from corroboration; require resolved-target ownership evidence before a university source is authoritative; once ownership is established, treat all of that university's official pages as one owner; and apply direct authoritative normative `verified` precedence before corroboration. |
| 2026-08-17 | Phase 2F cancellation propagation | A caller could abort while asynchronous target resolution was awaiting, after the pre-abort guard but before discovery registered its child controller/listener, allowing provider dispatch after cancellation. | Cancellation was checked only before an awaited boundary and assumed to remain unchanged across the await. | Re-check caller cancellation immediately after awaited setup/resolution boundaries and before registering downstream listeners or dispatching side effects; add a regression that aborts inside the awaited resolver and proves zero later provider calls. |
| 2026-08-17 | Phase 2F deterministic AI test seams | Injected reconciliation/explanation callbacks could report more actual attempts than the remaining stage/provider budget; accounting silently clamped usage while still accepting the payload, so tests could exercise histories production transport could never reach. | Test-seam accounting treated budget counters as telemetry instead of an enforceable execution invariant. | Route all injected structured-AI seams through one shared strict budget accountant that rejects wrong-stage/non-AI attempts and total/provider overruns before accepting synthetic payloads; test seams must be at least as constrained as production. |
| 2026-08-17 | Phase 3A catalog search ownership | A program-name/subject search returned matching programs but could omit their owning university because the university list applied the query independently before program ownership was projected back into visible results. | Search treated university and program result sets as independent filters even though program selection requires its owning application identity. | Build program matches against the country-scoped university set, then union matched program owners into visible universities; regression-test that every returned program's `universityId` resolves inside the same search result. |
| 2026-08-17 | Phase 3B dossier target relabeling | A valid internally consistent `ResearchResult` for a different university/program could be composed under the caller-selected catalog target because `ResearchResult` does not retain the original request target as a cross-record root. | The public projection trusted the selected catalog label and Phase 2's internal candidate/claim consistency without proving those two identity domains matched. | At the server-to-public dossier boundary, fail closed unless every final claim carries the selected application-owned university ID and appropriate selected program scope; university-only dossiers must not silently present program-scoped final claims. |
| 2026-08-17 | Phase 3 public DTO minimization/lifecycle | The browser-safe dossier schema initially allowed unused public sources and terminal run statuses that contradicted ready/unknown/incomplete category rows. | Production composer correctness was assumed to make redundant public-contract validation unnecessary. | Make the public DTO independently self-consistent: every exposed source must be referenced by a final claim, terminal status must agree with processed/incomplete rows, and terminal timestamps must be monotonic so malformed server responses fail before rendering. |
| 2026-08-17 | Stable supported-catalog links | Year-specific official course URLs can become archived/superseded while the institution publishes a stable current canonical course route. | Initial catalog verification accepted an official page without preferring the institution's stable non-year canonical route. | For application-owned catalog navigation metadata, prefer current stable canonical official course URLs over year-specific archived routes when both exist; re-check them before release without treating catalog verification date as evidence freshness. |
| 2026-08-17 | Model-specific subagent policy | GLM-5.3 Max child/subagent usage, including reviewer delegation, did not behave reliably enough for the user's intended workflow, while native GPT review-agent usage should remain available. | One global routing/reviewer policy was applied uniformly across main models with materially different delegation behavior. | When the active main model is GLM-5.3 Max (`zai-coding/glm-5.3`), perform all planning, implementation, verification, documentation, and final review in the main agent with zero subagents. Native OpenAI GPT models retain the final read-only `code-reviewer` step after local gates; scope subagent liveness rules only to models/tasks that permit children. |
| 2026-08-18 | Phase 3C rendered validation accessibility | Field errors were visible, but the target and free-text controls did not consistently reference their exact error messages; a group-level sensitive-input rejection also marked blank optional fields invalid. | Visual error rendering was implemented separately from the control's programmatic error relationship, and group-level server rejection was projected uniformly onto every field. | Give each rendered field error a stable ID and include it in the affected control's `aria-describedby`; set `aria-invalid` only on controls that actually contain rejected group input or have their own field error, and keep blank optional controls neutral. |
| 2026-08-18 | Phase 3C preserved-result state composition | A newer refresh error over a prior partial dossier could expose two identically named retries for different request snapshots; the prior dossier heading used the client snapshot label, and `Clear result` could restore instead of clear the visible result. | Preserved historical result state and the newer request/error state each owned overlapping UI actions/labels without one explicit presentation owner. | When old and new run state coexist, assign each visible action to exactly one immutable submission, suppress superseded duplicate actions, derive displayed result identity from the validated server dossier, and make control labels match reducer semantics. |
| 2026-08-18 | Playwright with an active Next.js dev workspace | Starting a second fresh `next dev` on another port still failed because Next.js serializes the workspace development build through `.next/dev`, and generated E2E source snapshots made root ESLint traversal unexpectedly expensive. | Port isolation was mistaken for build-directory isolation, and Git-ignore rules were assumed to imply ESLint-ignore behavior. | Never attach acceptance tests to an arbitrary existing dev server. If a developer server owns the workspace `.next/dev`, run the fresh E2E dev server from a process-unique ignored source snapshot (or another verified isolated build root), and explicitly ignore generated E2E/output directories in lint tooling. |
| 2026-08-18 | Recursive cleanup in test tooling | A Playwright-only environment identifier was interpolated into a disposable harness path and then passed to recursive `rmSync`; an inherited value containing traversal segments could resolve cleanup outside `output/playwright` and even outside the repository. | A supposedly internal environment value was treated as a safe path fragment, and the destructive target was not re-validated against its approved root immediately before cleanup. | Validate every externally inheritable path fragment, resolve the final cleanup target canonically, and fail closed unless it remains strictly inside the approved disposable root before any recursive deletion. Add a pure regression for traversal/absolute-path cases. |
| 2026-08-18 | Keyboard accessibility review scope | A broad Research keyboard/focus audit still missed bypassing the repeated sticky site navigation because tests began tabbing within the navigation itself. | Flow coverage focused on reachability of controls rather than the user's ability to skip repeated blocks before reaching page content. | For shared layouts, include a keyboard-visible skip link (or equivalent bypass) and a focusable main target, and regression-test the first-tab/activation path in addition to ordinary form/modal focus flows. |
| 2026-08-18 | Comparison scoring over free-form evidence properties | Phase 4 planning exposed that the validated public dossier still intentionally carries bounded free-form claim `property` strings, so fuzzy label matching or generic value parsing would let semantically different evidence silently enter a user-fit score. | Evidence extraction needs flexible source-facing property text, but deterministic scoring needs a much narrower application-owned semantic vocabulary. | Bridge free-form evidence into scoring through a closed, explicitly reviewed metric/alias registry with exact typed/unit/currency/period/evidence eligibility. Treat anything outside that registry as unscored coverage loss rather than fuzzy-matching, coercing, converting, or guessing it. |
| 2026-08-18 | Final verification after isolated Playwright runs | Root TypeScript failed on a stale pre-fix `output/playwright/phase3d-dev-app-*` source snapshot even though the canonical application source was already corrected. | Timed-out/continued browser invocations had left reproducible isolated source trees behind, and the root compiler still discovered those generated `.tsx` files. | When a root static gate reports errors under a disposable Playwright snapshot, verify the canonical path first. After browser processes finish, inspect and remove only validated inactive harness snapshots inside `output/playwright/` before final type/lint gates, or explicitly exclude that generated root if project architecture permits. |
| 2026-08-18 | Generated source snapshots and root static analysis | Cleaning stale Playwright snapshots fixed one TypeScript run but did not prevent the same false-source failure from recurring after future isolated E2E runs because root `tsconfig.json` still included `output/playwright/**/*.ts(x)`. | Ignored/generated source trees were excluded from Git and ESLint but not from TypeScript's broad recursive include. | If a reproducible test harness copies application source under a generated root, explicitly exclude that root from the canonical compiler/static-analysis program as well as Git/lint discovery; cleanup remains hygiene, not the correctness mechanism. Add a regression that reads the compiler configuration. |
| 2026-08-18 | Cross-dossier evidence identifiers | Comparison trade-offs flattened claim IDs from independently validated Research dossiers, so two targets could legally reuse one dossier-local claim ID and make evidence resolution ambiguous. | An identifier uniqueness guarantee scoped to one aggregate was accidentally treated as globally unique after combining multiple aggregates. | When composing cross-aggregate provenance, carry the aggregate/target identity together with the local identifier (for example `{ targetKey, claimId }`) and regression-test deliberate ID reuse across aggregates. |
| 2026-08-18 | Independent evidence-state presentation | A Compare category with both conflict and outdated flags displayed only conflict because the UI used mutually exclusive early returns for two orthogonal states. | Independent evidence conditions were modeled as exclusive presentation branches even though the public contract permits both flags simultaneously. | Render orthogonal evidence warnings independently unless the contract defines mutual exclusion, and add combined-state browser coverage rather than testing each flag only in isolation. |
| 2026-08-18 | Runtime versus tooling dependency classification | The `shadcn` CLI remained in production dependencies even though application/runtime code never imported it, so its CLI/toolchain dependency subtree was included in production installs unnecessarily. | Scaffold-time tooling was not reclassified after generated UI source became repository-owned. | During architecture/dependency audits, verify tooling packages with source/script reference checks plus `npm ls --omit=dev`; move build/scaffold-only tools to pinned development dependencies and validate lock consistency before treating runtime footprint as intentional. |
| 2026-08-19 | Phase 5 downstream evidence planning | Guide assessment correctly gated stale/conflicting/inferred facts, but budget, scholarship, fee, and deadline planners initially re-read raw dossier claims with separate substring/first-item logic and could turn ineligible evidence back into definitive actions. | A trust policy enforced in one consumer was assumed to protect sibling downstream consumers even though they independently traversed the same untrusted aggregate. | Centralize/reuse the same exact semantic, context, source, verification, and cardinality gates for every downstream factual consumer; regression-test that ineligible evidence cannot be "laundered" into risks, budgets, fees, or scheduled tasks through a parallel code path. |
| 2026-08-19 | Phase 5 preserved-result and correction ownership | Reducer state retained a prior Guide result during refresh, but the UI initially hid it while loading; separately, `unsupported-target` correction was keyed by the full Research context, so an intake/year edit could bypass rejection for the same program. | State-machine data ownership and UI presentation ownership were implemented separately, and a failure domain tied to target identity was modeled with an overly specific cache/request key. | Verify preserved state is actually rendered and evidence-usable during pending work, not merely stored. Scope correction/invalidation to the semantic identity that failed (program target here), while keeping cache reuse keys as narrow as needed for data freshness; test context-only edits cannot bypass target-local rejection. |
| 2026-08-19 | Closed semantic alias equality | Equivalent reviewed Guide facts such as GPA scale `4.0`/`4.00` or qualification/subject aliases could be treated as conflicting when singleton dedupe compared raw strings, while broad generic normalization would have widened the closed trust boundary. | Comparison equality was implemented below the semantic registry and therefore did not share the same reviewed canonical meaning used by assessment. | For deterministic singleton equality, canonicalize only through the same closed semantic alias registries used by evaluation; preserve unrecognized raw values and regression-test both equivalent reviewed aliases and non-reviewed lookalikes. |
| 2026-08-19 | Cancellation across post-response UI awaits | Guide cancellation could arrive after Research returned successfully but while evidence/result presentation awaited a frame, allowing the run to complete despite a visible Cancel action. | Abort ownership was checked around transport but not re-checked after a later awaited presentation boundary. | Re-check mounted/sequence/abort ownership after every awaited setup or presentation handoff and before caching, publishing state, or other side effects, even when the network operation has already completed. |
| 2026-08-19 | Application-owned navigation versus evidence provenance | A schema-valid Research dossier with correct target IDs could replace official university/program navigation URLs with hostile same-ID URLs, and context-rejected Guide evidence such as tuition, scholarship availability, or application fees could disappear even though it correctly failed deterministic eligibility. | Structural validation and deterministic rejection were incorrectly assumed to establish application ownership and user-visible provenance. | Rebind application-owned navigation metadata from the checked-in catalog at the presentation boundary, keep dossier source links source-owned, and retain exact rejected evidence refs in fail-closed/manual outcomes without making those refs eligible for deterministic conclusions or presenting rejected values as current. |
| 2026-08-19 | Phase 6 authentication planning against current provider guidance | Earlier future-auth notes required `HttpOnly` cookies categorically, but current Supabase SSR rich-client guidance requires browser access to its session cookies and therefore does not support that blanket claim. | A generic web-session hardening rule was written before the concrete authentication provider/session architecture was selected and then treated as provider-independent truth. | Re-verify authentication cookie/session semantics from the selected provider's current primary documentation before freezing security requirements. Preserve provider-independent invariants such as server-verified ownership, no credential Web Storage, strict CSP, least privilege, RLS, and explicit residual-risk language; do not claim a cookie attribute the chosen supported session model does not provide. |
| 2026-08-19 | Same-origin authentication callback on mixed local hostnames | A successful local Magic Link callback set session cookies on `127.0.0.1`, but an absolute redirect derived from the server's internally canonicalized request URL sent the browser to `localhost`, so the visible page appeared signed out while requests on the original origin were authenticated. | Same-origin navigation was reconstructed from server-visible absolute host text instead of preserving the browser origin after a cookie-setting callback. | For same-origin auth callbacks that set cookies, prefer a relative `Location` target and verify the session on a fresh same-origin request before opening private UI. Treat `127.0.0.1` and `localhost` as different cookie origins even when local tooling canonicalizes between them. |
| 2026-08-19 | Persisted database timestamp parsing | A successful `saved_artifacts` insert could be reported to the client as a generic 503 because PostgREST returned `timestamptz` with an explicit numeric offset while the response validator accepted only the narrower default ISO datetime form. | The trusted row schema was based on an assumed serialization shape instead of the database/API's actual wire representation. | Validate persisted-row timestamps against the real database/API representation; for Postgres/PostgREST `timestamptz`, accept explicit offsets (for example `z.iso.datetime({ offset: true })`) and regression-test a representative returned value so a committed mutation cannot be mistaken for failure during response parsing. |
| 2026-08-20 | Strict CSP bootstrap under Next.js development | A request-nonce `next/script` bootstrap intermittently hydrated with a different nonce and later produced development chunk failures during long Playwright runs, while isolated cases and production behavior were otherwise sound. | A framework-managed client script wrapper was used for a tiny fixed first-party bootstrap whose per-request nonce must remain identical from server markup through hydration. | For fixed bootstrap code under a strict request-nonce CSP, prefer a first-party static script authorized by the request nonce when it avoids framework hydration ownership; never relax CSP to hide a nonce mismatch, and prove the change in both dev and built-production browser security tests. |
| 2026-08-20 | Sequential batch stop and explicit retry | Compare correctly stopped dispatch after deployment 429/504, but retry selection considered only outcomes that already existed, so targets intentionally never dispatched after the stop could be omitted from the immutable retry snapshot. | Retry eligibility was derived from failure records rather than from the full immutable submission minus terminal successes/non-retryable corrections. | When a sequential batch may terminate early, derive retry work by iterating the original immutable selection in order: retry missing outcomes and retryable failures, preserve successful outcomes, and keep explicit non-retryable correction states excluded. |
| 2026-08-20 | Concurrent Playwright origin isolation | The main Playwright config accepted an isolated port, but Auth/Saved helpers still parsed Magic Links and opened secondary contexts against hard-coded port 3102, so an otherwise isolated run failed or contacted the wrong test server. | Test-server ownership was decentralized: one config resolved the port while helper code independently embedded the historical default origin. | Once a browser harness supports a configurable origin, derive every URL parser, secondary context, callback validator, and helper from one validated origin resolver; regression-test both the default and an explicit alternate port. |
